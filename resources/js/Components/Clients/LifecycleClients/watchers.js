import { Validate } from "@/Models/ValidateModel";
import { Exceptions } from "@/Excepciones/Excepciones";

export const clientWatchers =
{
    watch:
    {
        //TODO: Hacer los Watchers
    }
}
